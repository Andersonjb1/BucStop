# Environment variables for local development

# API Gateway
PORT=3000
NODE_ENV=development
DATABASE_URL=postgresql://bucstop_user:bucstop_password@localhost:5432/bucstop

# CIDR blocks for access control (comma-separated)
# For development, allow all IPs. In production, restrict to ETSU network
ALLOWED_CIDR_BLOCKS=0.0.0.0/0

# Device validation period in days (default: 365)
# How long a validated device can access the system from outside ETSU network
DEVICE_VALIDATION_DAYS=365

# Web App
VITE_API_URL=http://localhost:3000

# Snake Game Service
GAME_NAME=snake
GAME_VERSION=1.0.0
