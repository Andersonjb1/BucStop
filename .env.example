# Environment variables for local development

# API Gateway
PORT=3000
NODE_ENV=development
DATABASE_URL=postgresql://bucstop_user:bucstop_password@localhost:5432/bucstop

# CIDR blocks for access control (comma-separated)
# For development, allow all IPs. In production, restrict to ETSU network
ALLOWED_CIDR_BLOCKS=0.0.0.0/0

# Web App
VITE_API_URL=http://localhost:3000

# Snake Game Service
GAME_NAME=snake
GAME_VERSION=1.0.0
